"use strict";

const ZIP_LOCAL_FILE_HEADER = 0x04034b50;
const ZIP_CENTRAL_DIRECTORY_HEADER = 0x02014b50;
const ZIP_END_OF_CENTRAL_DIRECTORY = 0x06054b50;
const ZIP_MAX_ENTRIES = 100;
const ZIP_MAX_COMPRESSED_BYTES = 50 * 1024 * 1024;
const ZIP_MAX_UNCOMPRESSED_BYTES = 100 * 1024 * 1024;
const ZIP_ALLOWED_ASSET = /^(?:assets\/(?:[^/]+\/)*[^/]+\.(?:jpe?g|png|webp))$/iu;
const ZIP_INDEX = "index.md";
const OA_ORIGIN = "https://oa.omindos.ai";
let pendingKnowledgeAssets = [];
function zipError(message) { const error = new Error(message); error.name = "ZipKnowledgePackageError"; return error; }
function decodeZipName(bytes) { try { return new TextDecoder("utf-8", { fatal: true }).decode(bytes).normalize("NFC"); } catch { throw zipError("ZIP 内文件名必须使用 UTF-8 编码。"); } }
function normalizeZipPath(path) { const value = String(path || "").replace(/\\/gu, "/").replace(/^\.\//u, ""); if (!value || value.startsWith("/") || /^[A-Za-z]:\//u.test(value)) throw zipError("ZIP 内存在不安全的文件路径。"); const segments = value.split("/"); if (segments.some((segment) => !segment || segment === "." || segment === "..")) throw zipError("ZIP 内存在不安全的文件路径。"); return value; }
function findEndOfCentralDirectory(view) { const minimum = Math.max(0, view.byteLength - 65_557); for (let offset = view.byteLength - 22; offset >= minimum; offset -= 1) if (view.getUint32(offset, true) === ZIP_END_OF_CENTRAL_DIRECTORY) return offset; throw zipError("ZIP 文件结构无效或已损坏。"); }
function readZipEntries(arrayBuffer) { const view = new DataView(arrayBuffer); const eocd = findEndOfCentralDirectory(view); const disk = view.getUint16(eocd + 4, true); const centralDisk = view.getUint16(eocd + 6, true); const entriesOnDisk = view.getUint16(eocd + 8, true); const entryCount = view.getUint16(eocd + 10, true); const centralSize = view.getUint32(eocd + 12, true); const centralOffset = view.getUint32(eocd + 16, true); if (disk !== 0 || centralDisk !== 0 || entriesOnDisk !== entryCount) throw zipError("暂不支持分卷 ZIP 文件。"); if (entryCount < 1 || entryCount > ZIP_MAX_ENTRIES) throw zipError(`ZIP 内最多允许 ${ZIP_MAX_ENTRIES} 个文件。`); if (centralOffset + centralSize > eocd) throw zipError("ZIP 中央目录无效。"); const entries = []; let cursor = centralOffset; let compressedTotal = 0; let uncompressedTotal = 0; for (let index = 0; index < entryCount; index += 1) { if (cursor + 46 > view.byteLength || view.getUint32(cursor, true) !== ZIP_CENTRAL_DIRECTORY_HEADER) throw zipError("ZIP 中央目录条目无效。"); const flags = view.getUint16(cursor + 8, true); const method = view.getUint16(cursor + 10, true); const compressedSize = view.getUint32(cursor + 20, true); const uncompressedSize = view.getUint32(cursor + 24, true); const nameLength = view.getUint16(cursor + 28, true); const extraLength = view.getUint16(cursor + 30, true); const commentLength = view.getUint16(cursor + 32, true); const localOffset = view.getUint32(cursor + 42, true); const nameStart = cursor + 46; const nameEnd = nameStart + nameLength; if (nameEnd + extraLength + commentLength > view.byteLength) throw zipError("ZIP 文件名或扩展字段无效。"); if ((flags & 1) !== 0) throw zipError("暂不支持加密 ZIP 文件。"); if ((flags & 0x0800) === 0) throw zipError("ZIP 内文件名必须使用 UTF-8 编码。"); if (method !== 0 && method !== 8) throw zipError("ZIP 仅支持 Store 或 Deflate 压缩方式。"); const name = normalizeZipPath(decodeZipName(new Uint8Array(arrayBuffer, nameStart, nameLength))); compressedTotal += compressedSize; uncompressedTotal += uncompressedSize; if (compressedTotal > ZIP_MAX_COMPRESSED_BYTES || uncompressedTotal > ZIP_MAX_UNCOMPRESSED_BYTES) throw zipError("ZIP 解压后内容过大，请拆分后重试。"); entries.push({ name, method, compressedSize, uncompressedSize, localOffset }); cursor = nameEnd + extraLength + commentLength; } return entries; }
async function inflateRaw(bytes) { const stream = new Blob([bytes]).stream().pipeThrough(new DecompressionStream("deflate-raw")); return new Uint8Array(await new Response(stream).arrayBuffer()); }
async function extractZipEntry(arrayBuffer, entry) { const view = new DataView(arrayBuffer); const offset = entry.localOffset; if (offset + 30 > view.byteLength || view.getUint32(offset, true) !== ZIP_LOCAL_FILE_HEADER) throw zipError(`${entry.name}：ZIP 本地文件头无效。`); const start = offset + 30 + view.getUint16(offset + 26, true) + view.getUint16(offset + 28, true); const compressed = new Uint8Array(arrayBuffer, start, entry.compressedSize); const bytes = entry.method === 0 ? new Uint8Array(compressed) : await inflateRaw(compressed); if (bytes.byteLength !== entry.uncompressedSize) throw zipError(`${entry.name}：解压后大小校验失败。`); return bytes; }
function markdownAssetReferences(markdown) { const references = new Set(); const pattern = /!\[[^\]]*\]\(\s*(?:<([^>]+)>|([^\s)]+))(?:\s+["'][^"']*["'])?\s*\)/gu; for (const match of markdown.matchAll(pattern)) { const raw = (match[1] || match[2] || "").split(/[?#]/u, 1)[0]; let decoded = raw; try { decoded = decodeURIComponent(raw); } catch {} if (/^assets\//iu.test(decoded)) references.add(normalizeZipPath(decoded)); } return references; }
async function unpackKnowledgeZip(file) { if (file.size > ZIP_MAX_COMPRESSED_BYTES) throw zipError("ZIP 文件不能超过 50 MB，请压缩图片或拆分资料。"); const arrayBuffer = await file.arrayBuffer(); const entries = readZipEntries(arrayBuffer); const byName = new Map(entries.map((entry) => [entry.name.toLowerCase(), entry])); const indexEntry = byName.get(ZIP_INDEX); if (!indexEntry) throw zipError("ZIP 必须包含根目录 index.md。"); const indexBytes = await extractZipEntry(arrayBuffer, indexEntry); const markdown = new TextDecoder("utf-8", { fatal: true }).decode(indexBytes).replace(/^\uFEFF/u, ""); const referenced = markdownAssetReferences(markdown); const files = [new File([indexBytes], "index.md", { type: "text/markdown", lastModified: file.lastModified })]; pendingKnowledgeAssets = []; for (const path of [...referenced].sort()) { const entry = byName.get(path.toLowerCase()); if (!entry || !ZIP_ALLOWED_ASSET.test(path)) throw zipError(`index.md 引用了缺失或不支持的图片：${path}`); const bytes = await extractZipEntry(arrayBuffer, entry); const ext = path.split(".").at(-1).toLowerCase(); const type = ext === "png" ? "image/png" : ext === "webp" ? "image/webp" : "image/jpeg"; const asset = new File([bytes], path, { type, lastModified: file.lastModified }); pendingKnowledgeAssets.push(asset); } return files; }
function setInputFiles(input, files) { const transfer = new DataTransfer(); for (const file of files) transfer.items.add(file); input.files = transfer.files; }
async function uploadPendingKnowledgeAssets(receipt) { if (!pendingKnowledgeAssets.length) return { assetCount: 0 }; const itemId = String(receipt?.item?.id || receipt?.items?.[0]?.id || ""); const revisionId = String(receipt?.assetUpload?.revisionId || ""); const uploadToken = String(receipt?.assetUpload?.uploadToken || ""); if (!itemId || !revisionId || !uploadToken) throw new Error("OA 未返回图片上传会话，请重试。"); for (const asset of pendingKnowledgeAssets) { const response = await fetch(`${OA_ORIGIN}/api/knowledge/assets`, { method: "PUT", credentials: "include", headers: { "Content-Type": asset.type, "X-Knowledge-Item-Id": itemId, "X-Knowledge-Revision-Id": revisionId, "X-Knowledge-Upload-Token": uploadToken, "X-Knowledge-Asset-Path": asset.name }, body: asset, signal: AbortSignal.timeout(120_000) }); const result = await response.json().catch(() => ({})); if (!response.ok) throw new Error(result.error || `${asset.name} 上传失败。`); }
  const expectedPaths = pendingKnowledgeAssets.map((asset) => asset.name); const response = await fetch(`${OA_ORIGIN}/api/knowledge/assets/finalize`, { method: "POST", credentials: "include", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ itemId, revisionId, uploadToken, expectedPaths }), signal: AbortSignal.timeout(60_000) }); const result = await response.json().catch(() => ({})); if (!response.ok) throw new Error(result.error || "图片完整性确认失败。"); pendingKnowledgeAssets = []; return result; }
function enhanceZipInput(input) { if (!(input instanceof HTMLInputElement) || input.dataset.zipKnowledgeEnhanced === "1") return; input.dataset.zipKnowledgeEnhanced = "1"; input.setAttribute("accept", ".md,.zip,text/markdown,application/zip,application/x-zip-compressed"); }
async function interceptZipSelection(event) { const input = event.target; if (!(input instanceof HTMLInputElement) || input.id !== "document-file" || input.dataset.zipKnowledgeDispatch === "1") return; const selected = Array.from(input.files || []); const zipFiles = selected.filter((file) => file.name.toLowerCase().endsWith(".zip")); if (!zipFiles.length) { pendingKnowledgeAssets = []; return; } event.preventDefault(); event.stopImmediatePropagation(); try { const files = await unpackKnowledgeZip(zipFiles[0]); setInputFiles(input, files); input.dataset.zipKnowledgeDispatch = "1"; input.dispatchEvent(new Event("change", { bubbles: true })); } catch (error) { pendingKnowledgeAssets = []; window.alert(error instanceof Error ? error.message : "ZIP 解析失败，请检查知识包结构。"); input.value = ""; } finally { delete input.dataset.zipKnowledgeDispatch; } }
function refreshKnowledgeUploadUi() { const input = document.getElementById("document-file"); if (input) enhanceZipInput(input); }
window.unpackKnowledgeZip = unpackKnowledgeZip;
window.uploadPendingKnowledgeAssets = uploadPendingKnowledgeAssets;
document.addEventListener("change", interceptZipSelection, true);
const observer = new MutationObserver(refreshKnowledgeUploadUi); observer.observe(document.documentElement, { childList: true, subtree: true }); refreshKnowledgeUploadUi();
