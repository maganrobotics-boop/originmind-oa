# Client asset notices

The ARTS Robotics AI assistant browser interface is implemented with standard
HTML, CSS, JavaScript, and system fonts. The current public frontend bundle does
not include third-party JavaScript, CSS, font, image, or icon packages.

Server-side development and deployment dependencies are recorded separately in
`package.json` and `package-lock.json`; they are not shipped as browser
assets.
